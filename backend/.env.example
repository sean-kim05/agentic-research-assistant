# Copy this file to ".env" and fill in real values. NEVER commit the real .env.

# Origin(s) allowed to call this API from the browser (comma-separated).
# The Next.js dev server runs on http://localhost:3000 by default.
FRONTEND_ORIGINS=http://localhost:3000

# --- Phase 2 keys (keep these SERVER-SIDE only) ---
VOYAGE_API_KEY=your-voyage-key-here        # https://dashboard.voyageai.com
PINECONE_API_KEY=your-pinecone-key-here    # https://app.pinecone.io

# Pinecone index settings (defaults work on the free tier).
PINECONE_INDEX=research-assistant
PINECONE_CLOUD=aws
PINECONE_REGION=us-east-1

# --- Later phases ---
# ANTHROPIC_API_KEY=
# TAVILY_API_KEY=
