# Phase 0 dependencies (kept minimal on purpose).
fastapi
uvicorn
python-dotenv

# Phase 1 dependencies.
pypdf              # extract text from PDF files
python-multipart   # lets FastAPI receive uploaded files (multipart/form-data)

# Phase 2 dependencies.
voyageai           # embeddings (voyage-3)
pinecone           # vector database (semantic search)

# Later phases will add: anthropic, tavily-python
